import Fade from "./Fade";
import { ENTERING, EXITING } from "solid-react-transition";
const fadeStyles = {
    [ENTERING]: "showing",
    [EXITING]: "showing show",
};
const ToastFade = (props) => <Fade {...props} transitionClasses={fadeStyles}/>;
export default ToastFade;
