import { JSX } from "solid-js";
import { TransitionProps } from "solid-react-transition";
export type FadeProps = Omit<TransitionProps, "addEndListener"> & {
    class?: string;
    transitionClasses?: Record<string, string>;
};
declare const Fade: (p: FadeProps) => JSX.Element;
export default Fade;
