import { JSX } from "solid-js";
/**
 * Solid event handlers can be a plain callback OR a tuple [handler, item].
 * This function calls the handler appropriately (if defined).
 */
export declare function callEventHandler(h: JSX.EventHandlerUnion<any, any> | undefined, e: Event): {
    isPropagationStopped: boolean;
};
export declare function resolveClasses(el: Element, prev: string, now: string): void;
